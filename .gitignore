_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor
.DS_Store

# Local-only working notes -- never meant for this public repo.
# Deliberately untracked: LinkedIn drafts (unreviewed/unapproved copy)
# and competitive-intelligence research notes.
/social/
/research/

# Client/proposal source material (RFPs, SOWs, briefing decks) -- confidential,
# never for this public repo. Read for pattern/theme extraction only; never
# committed, never quoted verbatim, never client-identified in published content.
/references/
